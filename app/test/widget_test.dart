// Placeholder — app requires device context for wifi/audio; UI tests run on device.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () => expect(true, isTrue));
}
